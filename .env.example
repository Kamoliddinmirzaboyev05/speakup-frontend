# Backend API base URL. Leave empty in dev to use the Vite proxy (-> http://localhost:8000).
# In production set the full origin, e.g. https://api.yourdomain.com
VITE_API_URL=

# Bot username (no @). Used to build referral links in the Invite screen.
VITE_BOT_USERNAME=yourbot
