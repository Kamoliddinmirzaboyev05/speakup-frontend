# Production build env (loaded automatically by `vite build` on Vercel).
# API base is public, not a secret. Same-origin is not used in prod because the
# Mini App is served from Vercel while the API lives on the DO server.
VITE_API_URL=https://speakupapi.webportfolio.uz
VITE_BOT_USERNAME=speakuprobot
